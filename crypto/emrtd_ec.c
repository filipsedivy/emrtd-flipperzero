/*
 * SPDX-License-Identifier: MIT
 * Copyright (c) 2026 Filip Sedivy
 *
 * The domain parameters below are generated from the published curve
 * definitions (FIPS 186-4 for the NIST curves, RFC 5639 for brainpool) and are
 * checked by the host test suite: p prime, G on the curve, n*G = O.
 */

/*
 * Before any mbed TLS header, and only in this file: it lets
 * emrtd_ec_point_load_xy() assign a point's coordinates by name rather than
 * formatting them as hexadecimal for mbedtls_ecp_point_read_string(). The
 * macro renames struct members; it does not change their layout, so nothing
 * this file hands to another translation unit is affected.
 */
#define MBEDTLS_ALLOW_PRIVATE_ACCESS

#include "emrtd_ec.h"

#include "../emrtd_wipe.h"

#include <string.h>

static const uint8_t secp192r1_p[24] = {
    0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF,
    0xFF, 0xFF, 0xFF, 0xFE, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF,
};
static const uint8_t secp192r1_a[24] = {
    0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF,
    0xFF, 0xFF, 0xFF, 0xFE, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFC,
};
static const uint8_t secp192r1_b[24] = {
    0x64, 0x21, 0x05, 0x19, 0xE5, 0x9C, 0x80, 0xE7, 0x0F, 0xA7, 0xE9, 0xAB,
    0x72, 0x24, 0x30, 0x49, 0xFE, 0xB8, 0xDE, 0xEC, 0xC1, 0x46, 0xB9, 0xB1,
};
static const uint8_t secp192r1_gx[24] = {
    0x18, 0x8D, 0xA8, 0x0E, 0xB0, 0x30, 0x90, 0xF6, 0x7C, 0xBF, 0x20, 0xEB,
    0x43, 0xA1, 0x88, 0x00, 0xF4, 0xFF, 0x0A, 0xFD, 0x82, 0xFF, 0x10, 0x12,
};
static const uint8_t secp192r1_gy[24] = {
    0x07, 0x19, 0x2B, 0x95, 0xFF, 0xC8, 0xDA, 0x78, 0x63, 0x10, 0x11, 0xED,
    0x6B, 0x24, 0xCD, 0xD5, 0x73, 0xF9, 0x77, 0xA1, 0x1E, 0x79, 0x48, 0x11,
};
static const uint8_t secp192r1_n[24] = {
    0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF,
    0x99, 0xDE, 0xF8, 0x36, 0x14, 0x6B, 0xC9, 0xB1, 0xB4, 0xD2, 0x28, 0x31,
};

static const uint8_t brainpoolp192r1_p[24] = {
    0xC3, 0x02, 0xF4, 0x1D, 0x93, 0x2A, 0x36, 0xCD, 0xA7, 0xA3, 0x46, 0x30,
    0x93, 0xD1, 0x8D, 0xB7, 0x8F, 0xCE, 0x47, 0x6D, 0xE1, 0xA8, 0x62, 0x97,
};
static const uint8_t brainpoolp192r1_a[24] = {
    0x6A, 0x91, 0x17, 0x40, 0x76, 0xB1, 0xE0, 0xE1, 0x9C, 0x39, 0xC0, 0x31,
    0xFE, 0x86, 0x85, 0xC1, 0xCA, 0xE0, 0x40, 0xE5, 0xC6, 0x9A, 0x28, 0xEF,
};
static const uint8_t brainpoolp192r1_b[24] = {
    0x46, 0x9A, 0x28, 0xEF, 0x7C, 0x28, 0xCC, 0xA3, 0xDC, 0x72, 0x1D, 0x04,
    0x4F, 0x44, 0x96, 0xBC, 0xCA, 0x7E, 0xF4, 0x14, 0x6F, 0xBF, 0x25, 0xC9,
};
static const uint8_t brainpoolp192r1_gx[24] = {
    0xC0, 0xA0, 0x64, 0x7E, 0xAA, 0xB6, 0xA4, 0x87, 0x53, 0xB0, 0x33, 0xC5,
    0x6C, 0xB0, 0xF0, 0x90, 0x0A, 0x2F, 0x5C, 0x48, 0x53, 0x37, 0x5F, 0xD6,
};
static const uint8_t brainpoolp192r1_gy[24] = {
    0x14, 0xB6, 0x90, 0x86, 0x6A, 0xBD, 0x5B, 0xB8, 0x8B, 0x5F, 0x48, 0x28,
    0xC1, 0x49, 0x00, 0x02, 0xE6, 0x77, 0x3F, 0xA2, 0xFA, 0x29, 0x9B, 0x8F,
};
static const uint8_t brainpoolp192r1_n[24] = {
    0xC3, 0x02, 0xF4, 0x1D, 0x93, 0x2A, 0x36, 0xCD, 0xA7, 0xA3, 0x46, 0x2F,
    0x9E, 0x9E, 0x91, 0x6B, 0x5B, 0xE8, 0xF1, 0x02, 0x9A, 0xC4, 0xAC, 0xC1,
};

static const uint8_t secp224r1_p[28] = {
    0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF,
    0xFF, 0xFF, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x01,
};
static const uint8_t secp224r1_a[28] = {
    0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF,
    0xFF, 0xFE, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFE,
};
static const uint8_t secp224r1_b[28] = {
    0xB4, 0x05, 0x0A, 0x85, 0x0C, 0x04, 0xB3, 0xAB, 0xF5, 0x41, 0x32, 0x56, 0x50, 0x44,
    0xB0, 0xB7, 0xD7, 0xBF, 0xD8, 0xBA, 0x27, 0x0B, 0x39, 0x43, 0x23, 0x55, 0xFF, 0xB4,
};
static const uint8_t secp224r1_gx[28] = {
    0xB7, 0x0E, 0x0C, 0xBD, 0x6B, 0xB4, 0xBF, 0x7F, 0x32, 0x13, 0x90, 0xB9, 0x4A, 0x03,
    0xC1, 0xD3, 0x56, 0xC2, 0x11, 0x22, 0x34, 0x32, 0x80, 0xD6, 0x11, 0x5C, 0x1D, 0x21,
};
static const uint8_t secp224r1_gy[28] = {
    0xBD, 0x37, 0x63, 0x88, 0xB5, 0xF7, 0x23, 0xFB, 0x4C, 0x22, 0xDF, 0xE6, 0xCD, 0x43,
    0x75, 0xA0, 0x5A, 0x07, 0x47, 0x64, 0x44, 0xD5, 0x81, 0x99, 0x85, 0x00, 0x7E, 0x34,
};
static const uint8_t secp224r1_n[28] = {
    0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF,
    0x16, 0xA2, 0xE0, 0xB8, 0xF0, 0x3E, 0x13, 0xDD, 0x29, 0x45, 0x5C, 0x5C, 0x2A, 0x3D,
};

static const uint8_t brainpoolp224r1_p[28] = {
    0xD7, 0xC1, 0x34, 0xAA, 0x26, 0x43, 0x66, 0x86, 0x2A, 0x18, 0x30, 0x25, 0x75, 0xD1,
    0xD7, 0x87, 0xB0, 0x9F, 0x07, 0x57, 0x97, 0xDA, 0x89, 0xF5, 0x7E, 0xC8, 0xC0, 0xFF,
};
static const uint8_t brainpoolp224r1_a[28] = {
    0x68, 0xA5, 0xE6, 0x2C, 0xA9, 0xCE, 0x6C, 0x1C, 0x29, 0x98, 0x03, 0xA6, 0xC1, 0x53,
    0x0B, 0x51, 0x4E, 0x18, 0x2A, 0xD8, 0xB0, 0x04, 0x2A, 0x59, 0xCA, 0xD2, 0x9F, 0x43,
};
static const uint8_t brainpoolp224r1_b[28] = {
    0x25, 0x80, 0xF6, 0x3C, 0xCF, 0xE4, 0x41, 0x38, 0x87, 0x07, 0x13, 0xB1, 0xA9, 0x23,
    0x69, 0xE3, 0x3E, 0x21, 0x35, 0xD2, 0x66, 0xDB, 0xB3, 0x72, 0x38, 0x6C, 0x40, 0x0B,
};
static const uint8_t brainpoolp224r1_gx[28] = {
    0x0D, 0x90, 0x29, 0xAD, 0x2C, 0x7E, 0x5C, 0xF4, 0x34, 0x08, 0x23, 0xB2, 0xA8, 0x7D,
    0xC6, 0x8C, 0x9E, 0x4C, 0xE3, 0x17, 0x4C, 0x1E, 0x6E, 0xFD, 0xEE, 0x12, 0xC0, 0x7D,
};
static const uint8_t brainpoolp224r1_gy[28] = {
    0x58, 0xAA, 0x56, 0xF7, 0x72, 0xC0, 0x72, 0x6F, 0x24, 0xC6, 0xB8, 0x9E, 0x4E, 0xCD,
    0xAC, 0x24, 0x35, 0x4B, 0x9E, 0x99, 0xCA, 0xA3, 0xF6, 0xD3, 0x76, 0x14, 0x02, 0xCD,
};
static const uint8_t brainpoolp224r1_n[28] = {
    0xD7, 0xC1, 0x34, 0xAA, 0x26, 0x43, 0x66, 0x86, 0x2A, 0x18, 0x30, 0x25, 0x75, 0xD0,
    0xFB, 0x98, 0xD1, 0x16, 0xBC, 0x4B, 0x6D, 0xDE, 0xBC, 0xA3, 0xA5, 0xA7, 0x93, 0x9F,
};

static const uint8_t secp256r1_p[32] = {
    0xFF, 0xFF, 0xFF, 0xFF, 0x00, 0x00, 0x00, 0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
    0x00, 0x00, 0x00, 0x00, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF,
};
static const uint8_t secp256r1_a[32] = {
    0xFF, 0xFF, 0xFF, 0xFF, 0x00, 0x00, 0x00, 0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
    0x00, 0x00, 0x00, 0x00, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFC,
};
static const uint8_t secp256r1_b[32] = {
    0x5A, 0xC6, 0x35, 0xD8, 0xAA, 0x3A, 0x93, 0xE7, 0xB3, 0xEB, 0xBD, 0x55, 0x76, 0x98, 0x86, 0xBC,
    0x65, 0x1D, 0x06, 0xB0, 0xCC, 0x53, 0xB0, 0xF6, 0x3B, 0xCE, 0x3C, 0x3E, 0x27, 0xD2, 0x60, 0x4B,
};
static const uint8_t secp256r1_gx[32] = {
    0x6B, 0x17, 0xD1, 0xF2, 0xE1, 0x2C, 0x42, 0x47, 0xF8, 0xBC, 0xE6, 0xE5, 0x63, 0xA4, 0x40, 0xF2,
    0x77, 0x03, 0x7D, 0x81, 0x2D, 0xEB, 0x33, 0xA0, 0xF4, 0xA1, 0x39, 0x45, 0xD8, 0x98, 0xC2, 0x96,
};
static const uint8_t secp256r1_gy[32] = {
    0x4F, 0xE3, 0x42, 0xE2, 0xFE, 0x1A, 0x7F, 0x9B, 0x8E, 0xE7, 0xEB, 0x4A, 0x7C, 0x0F, 0x9E, 0x16,
    0x2B, 0xCE, 0x33, 0x57, 0x6B, 0x31, 0x5E, 0xCE, 0xCB, 0xB6, 0x40, 0x68, 0x37, 0xBF, 0x51, 0xF5,
};
static const uint8_t secp256r1_n[32] = {
    0xFF, 0xFF, 0xFF, 0xFF, 0x00, 0x00, 0x00, 0x00, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF,
    0xBC, 0xE6, 0xFA, 0xAD, 0xA7, 0x17, 0x9E, 0x84, 0xF3, 0xB9, 0xCA, 0xC2, 0xFC, 0x63, 0x25, 0x51,
};

static const uint8_t brainpoolp256r1_p[32] = {
    0xA9, 0xFB, 0x57, 0xDB, 0xA1, 0xEE, 0xA9, 0xBC, 0x3E, 0x66, 0x0A, 0x90, 0x9D, 0x83, 0x8D, 0x72,
    0x6E, 0x3B, 0xF6, 0x23, 0xD5, 0x26, 0x20, 0x28, 0x20, 0x13, 0x48, 0x1D, 0x1F, 0x6E, 0x53, 0x77,
};
static const uint8_t brainpoolp256r1_a[32] = {
    0x7D, 0x5A, 0x09, 0x75, 0xFC, 0x2C, 0x30, 0x57, 0xEE, 0xF6, 0x75, 0x30, 0x41, 0x7A, 0xFF, 0xE7,
    0xFB, 0x80, 0x55, 0xC1, 0x26, 0xDC, 0x5C, 0x6C, 0xE9, 0x4A, 0x4B, 0x44, 0xF3, 0x30, 0xB5, 0xD9,
};
static const uint8_t brainpoolp256r1_b[32] = {
    0x26, 0xDC, 0x5C, 0x6C, 0xE9, 0x4A, 0x4B, 0x44, 0xF3, 0x30, 0xB5, 0xD9, 0xBB, 0xD7, 0x7C, 0xBF,
    0x95, 0x84, 0x16, 0x29, 0x5C, 0xF7, 0xE1, 0xCE, 0x6B, 0xCC, 0xDC, 0x18, 0xFF, 0x8C, 0x07, 0xB6,
};
static const uint8_t brainpoolp256r1_gx[32] = {
    0x8B, 0xD2, 0xAE, 0xB9, 0xCB, 0x7E, 0x57, 0xCB, 0x2C, 0x4B, 0x48, 0x2F, 0xFC, 0x81, 0xB7, 0xAF,
    0xB9, 0xDE, 0x27, 0xE1, 0xE3, 0xBD, 0x23, 0xC2, 0x3A, 0x44, 0x53, 0xBD, 0x9A, 0xCE, 0x32, 0x62,
};
static const uint8_t brainpoolp256r1_gy[32] = {
    0x54, 0x7E, 0xF8, 0x35, 0xC3, 0xDA, 0xC4, 0xFD, 0x97, 0xF8, 0x46, 0x1A, 0x14, 0x61, 0x1D, 0xC9,
    0xC2, 0x77, 0x45, 0x13, 0x2D, 0xED, 0x8E, 0x54, 0x5C, 0x1D, 0x54, 0xC7, 0x2F, 0x04, 0x69, 0x97,
};
static const uint8_t brainpoolp256r1_n[32] = {
    0xA9, 0xFB, 0x57, 0xDB, 0xA1, 0xEE, 0xA9, 0xBC, 0x3E, 0x66, 0x0A, 0x90, 0x9D, 0x83, 0x8D, 0x71,
    0x8C, 0x39, 0x7A, 0xA3, 0xB5, 0x61, 0xA6, 0xF7, 0x90, 0x1E, 0x0E, 0x82, 0x97, 0x48, 0x56, 0xA7,
};

/*
 * The curves above 256 bits carry no parameters. MBEDTLS_ECP_MAX_BITS is 256
 * in this firmware's mbed TLS, so emrtd_ec_curve_supported() refuses them
 * before a byte is read, and the prime, coefficients, generator and order for
 * brainpoolP320r1, NIST P-384, brainpoolP384r1, brainpoolP512r1 and NIST P-521
 * were 1,596 bytes of a binary that is loaded into RAM. They are listed in the
 * table below by name alone, which is all a refusal needs to name what it
 * refused.
 */

static const EmrtdCurve emrtd_curves[] = {
    {
        .param_id = 8,
        .name = "NIST P-192",
        .bits = 192,
        .size = 24,
        .p = secp192r1_p,
        .a = secp192r1_a,
        .b = secp192r1_b,
        .gx = secp192r1_gx,
        .gy = secp192r1_gy,
        .n = secp192r1_n,
    },
    {
        .param_id = 9,
        .name = "brainpoolP192r1",
        .bits = 192,
        .size = 24,
        .p = brainpoolp192r1_p,
        .a = brainpoolp192r1_a,
        .b = brainpoolp192r1_b,
        .gx = brainpoolp192r1_gx,
        .gy = brainpoolp192r1_gy,
        .n = brainpoolp192r1_n,
    },
    {
        .param_id = 10,
        .name = "NIST P-224",
        .bits = 224,
        .size = 28,
        .p = secp224r1_p,
        .a = secp224r1_a,
        .b = secp224r1_b,
        .gx = secp224r1_gx,
        .gy = secp224r1_gy,
        .n = secp224r1_n,
    },
    {
        .param_id = 11,
        .name = "brainpoolP224r1",
        .bits = 224,
        .size = 28,
        .p = brainpoolp224r1_p,
        .a = brainpoolp224r1_a,
        .b = brainpoolp224r1_b,
        .gx = brainpoolp224r1_gx,
        .gy = brainpoolp224r1_gy,
        .n = brainpoolp224r1_n,
    },
    {
        .param_id = 12,
        .name = "NIST P-256",
        .bits = 256,
        .size = 32,
        .p = secp256r1_p,
        .a = secp256r1_a,
        .b = secp256r1_b,
        .gx = secp256r1_gx,
        .gy = secp256r1_gy,
        .n = secp256r1_n,
    },
    {
        .param_id = 13,
        .name = "brainpoolP256r1",
        .bits = 256,
        .size = 32,
        .p = brainpoolp256r1_p,
        .a = brainpoolp256r1_a,
        .b = brainpoolp256r1_b,
        .gx = brainpoolp256r1_gx,
        .gy = brainpoolp256r1_gy,
        .n = brainpoolp256r1_n,
    },
    /*
     * Above this line the parameters are carried; below it they are not.
     *
     * MBEDTLS_ECP_MAX_BITS is 256 in the firmware's mbed TLS, because only
     * secp256r1 is compiled in and COMB_MAX_D sizes a stack array in
     * ecp_mul_comb() from it. So these five curves are refused by
     * emrtd_ec_curve_supported() before a single one of their bytes is read,
     * and carrying 1,596 bytes of prime, coefficients, generator and order
     * for them buys nothing - in a binary that is loaded into RAM, on a
     * device whose whole heap is 186 kB.
     *
     * The identity is kept, because that is what the entries are for: a chip
     * that asks for brainpoolP512r1 gets told it asked for brainpoolP512r1,
     * rather than being met with a number. See emrtd_ec.h.
     */
    {
        .param_id = 14,
        .name = "brainpoolP320r1",
        .bits = 320,
        .size = 40,
    },
    {
        .param_id = 15,
        .name = "NIST P-384",
        .bits = 384,
        .size = 48,
    },
    {
        .param_id = 16,
        .name = "brainpoolP384r1",
        .bits = 384,
        .size = 48,
    },
    {
        .param_id = 17,
        .name = "brainpoolP512r1",
        .bits = 512,
        .size = 64,
    },
    {
        .param_id = 18,
        .name = "NIST P-521",
        .bits = 521,
        .size = 66,
    },
};

/*
 * Load a point from its uncompressed encoding without going through
 * mbedtls_ecp_point_read_binary().
 *
 * That function also accepts the compressed forms 02 and 03, and recovering
 * the missing coordinate means taking a modular square root - which reaches
 * mbedtls_mpi_exp_mod(), whose Montgomery multiplication depends on
 * mbedtls_ct_memcpy_if(). That symbol is not in the firmware's mbed TLS
 * archive and is not exported by the firmware either, so merely linking the
 * decompression path makes the application unloadable. See docs/platform.md.
 *
 * ICAO 9303-11 mandates the uncompressed encoding of TR-03111, so nothing is
 * lost: the coordinates are assigned straight from their big endian bytes.
 *
 * They used to be formatted as hexadecimal and handed to
 * mbedtls_ecp_point_read_string(), because a point's coordinates are declared
 * MBEDTLS_PRIVATE. That cost 490 bytes of code - mbedtls_mpi_read_string() and
 * its digit parser, linked for nothing else - and put two 133 byte buffers on
 * the stack of a function called during PACE. MBEDTLS_ALLOW_PRIVATE_ACCESS at
 * the top of this file removes both. It is exactly what
 * mbedtls_ecp_point_read_string() does internally: read X, read Y, set Z to 1.
 */
static int emrtd_ec_point_load_xy(
    mbedtls_ecp_point* point,
    const uint8_t* x,
    const uint8_t* y,
    size_t size) {
    if(size > EMRTD_EC_COORD_MAX) {
        return MBEDTLS_ERR_ECP_BAD_INPUT_DATA;
    }

    int ret;
    if((ret = mbedtls_mpi_read_binary(&point->X, x, size)) != 0) {
        return ret;
    }
    if((ret = mbedtls_mpi_read_binary(&point->Y, y, size)) != 0) {
        return ret;
    }
    return mbedtls_mpi_lset(&point->Z, 1);
}

size_t emrtd_ec_curve_count(void) {
    return sizeof(emrtd_curves) / sizeof(emrtd_curves[0]);
}

const EmrtdCurve* emrtd_ec_curve_at(size_t index) {
    return index < emrtd_ec_curve_count() ? &emrtd_curves[index] : NULL;
}

const EmrtdCurve* emrtd_ec_curve_by_param_id(uint8_t param_id) {
    for(size_t i = 0; i < emrtd_ec_curve_count(); i++) {
        if(emrtd_curves[i].param_id == param_id) {
            return &emrtd_curves[i];
        }
    }
    return NULL;
}

bool emrtd_ec_curve_supported(const EmrtdCurve* curve) {
    /*
     * Both halves, and not because one implies the other today. The table
     * carries parameters only for the curves this build can compute, so the
     * bit length and the presence of the bytes are two statements of the same
     * fact - and the day one of them is edited without the other, this asks
     * the question that cannot be got wrong.
     */
    return curve != NULL && curve->p != NULL && curve->bits <= MBEDTLS_ECP_MAX_BITS;
}

int emrtd_ec_group_load(mbedtls_ecp_group* grp, const EmrtdCurve* curve) {
    if(grp == NULL || !emrtd_ec_curve_supported(curve)) {
        return MBEDTLS_ERR_ECP_BAD_INPUT_DATA;
    }

    int ret;
    mbedtls_ecp_group_init(grp);

    /*
     * MBEDTLS_ECP_DP_NONE keeps mbed TLS from assuming one of its built in
     * curves; grp->modp stays NULL, which selects the generic modular
     * reduction. The cofactor is deliberately left at the zero set by
     * mbedtls_ecp_group_init(): mbedtls_ecp_group_free() only releases the
     * parameter MPIs when it is not one, and ours are heap allocated. The
     * field takes no part in the arithmetic (ecp.c reads it only there).
     */
    grp->id = MBEDTLS_ECP_DP_NONE;

    if((ret = mbedtls_mpi_read_binary(&grp->P, curve->p, curve->size)) != 0) goto fail;
    if((ret = mbedtls_mpi_read_binary(&grp->A, curve->a, curve->size)) != 0) goto fail;
    if((ret = mbedtls_mpi_read_binary(&grp->B, curve->b, curve->size)) != 0) goto fail;
    if((ret = mbedtls_mpi_read_binary(&grp->N, curve->n, curve->size)) != 0) goto fail;

    grp->pbits = mbedtls_mpi_bitlen(&grp->P);
    grp->nbits = mbedtls_mpi_bitlen(&grp->N);

    /*
     * The coordinates of an mbedtls_ecp_point are private in mbed TLS 3, so
     * the generator is assigned through the one public call that takes them
     * directly. See emrtd_ec_point_load_xy() for why the binary reader is
     * avoided throughout this file.
     */
    if((ret = emrtd_ec_point_load_xy(&grp->G, curve->gx, curve->gy, curve->size)) != 0) {
        goto fail;
    }

    return 0;

fail:
    mbedtls_ecp_group_free(grp);
    return ret;
}

int emrtd_ec_point_read(
    mbedtls_ecp_group* grp,
    mbedtls_ecp_point* point,
    const uint8_t* data,
    size_t len) {
    if(len < 1 || data[0] != 0x04) {
        /* ICAO 9303-11 mandates the uncompressed encoding of TR-03111. */
        return MBEDTLS_ERR_ECP_FEATURE_UNAVAILABLE;
    }
    const size_t size = (mbedtls_mpi_bitlen(&grp->P) + 7) / 8;
    if(len != 1 + 2 * size) {
        return MBEDTLS_ERR_ECP_BAD_INPUT_DATA;
    }

    int ret = emrtd_ec_point_load_xy(point, data + 1, data + 1 + size, size);
    if(ret != 0) {
        return ret;
    }
    /* A point off the curve would leak the private scalar, so refuse it. */
    return mbedtls_ecp_check_pubkey(grp, point);
}

int emrtd_ec_point_write(
    const mbedtls_ecp_group* grp,
    const mbedtls_ecp_point* point,
    uint8_t* out,
    size_t out_size,
    size_t* out_len) {
    return mbedtls_ecp_point_write_binary(
        grp, point, MBEDTLS_ECP_PF_UNCOMPRESSED, out_len, out, out_size);
}

int emrtd_ec_point_x(
    const mbedtls_ecp_group* grp,
    const mbedtls_ecp_point* point,
    size_t coord_size,
    uint8_t* out) {
    /*
     * Both coordinates of the shared point land here and only x is wanted, so
     * y would otherwise be left on the stack of the NFC thread - whose stack
     * is a heap block that the allocator hands out again without zeroing.
     */
    uint8_t encoded[1 + 2 * EMRTD_EC_COORD_MAX];
    size_t olen = 0;
    int ret = emrtd_ec_point_write(grp, point, encoded, sizeof(encoded), &olen);
    if(ret == 0 && olen != 1 + 2 * coord_size) {
        ret = MBEDTLS_ERR_ECP_BAD_INPUT_DATA;
    }
    if(ret == 0) {
        memcpy(out, encoded + 1, coord_size);
    }
    emrtd_secure_wipe(encoded, sizeof(encoded));
    return ret;
}
